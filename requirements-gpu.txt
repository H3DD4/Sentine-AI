# Install the base application dependencies first, then use the CUDA build of
# PyTorch instead of the default CPU build selected by pip.
-r requirements.txt
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
